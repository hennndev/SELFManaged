/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fisika.uad.ac.id', 'lh3.googleusercontent.com', 'res.cloudinary.com']
    }
}

module.exports = nextConfig
